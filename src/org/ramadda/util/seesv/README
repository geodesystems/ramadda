
Requirements:
Java 1.8 or higher installed. Set the JAVA environment variable to
point to the java runtime.

Set the environment variable
export SEESV=/path/to/this/directory

Help:
seesv.html

or:
https://ramadda.org/repository/userguide/seesv.html


To run:
sh seesv.sh <commands>

To show help:
sh seesv.sh -help


To show help on a topic:
sh seesv.sh -help:some_topic

To see the version:
sh seesv.sh -version

To run, e.g.:
sh seesv.sh -columns "0,1,2" -print input.csv


#############################################
## Running on Ubuntu
#############################################

Note: On Ubuntu sh defaults to dash which does not support set -o pipefail
You can still run with sh but this pipefail failure will be ignored
If you want to you can run with:
bash seesv.sh






