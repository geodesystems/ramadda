
This uses the IDV generate.tcl script to create the workshop 
content and places it in htdocs/workshop directory. 

The ant script calls tclsh. See the top level README to see how to set
the path to tclsh.  To generate the docs, run:

   ant workshop

To install the userguide plugin, run:

   ant 

(default target)

To generate and install the workshop plugin, run:

   ant all

To view the content in RAMADDA just go to:
http://localhost:8080/repository/workshop/index.html

You can also view the generated html files directly in:
htdocs/workshop/index.html





